Requirements Review
    No duplicate weeks (each week should be unique by start/stop date). (This should be checked first)
    Should always contain exactly 52 weeks of data (no more, no less).
    The most recent week should always be the current week.